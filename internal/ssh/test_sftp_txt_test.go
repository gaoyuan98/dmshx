package ssh

import (
	"testing"
)

func TestQuerySftp(t *testing.T) {
	/*	sshConfig := &ssh.ClientConfig{
		User: "root",
		Auth: []ssh.AuthMethod{
			ssh.Password("gaoyuan123#"), // 或 ssh.PublicKeys(...)
		},
		HostKeyCallback: ssh.InsecureIgnoreHostKey(),
		Timeout:         5 * time.Second,
	}*/

}
